# Variáveis de ambiente para o bot de cupons Seven Knights Re:BIRTH

# Webhook do Discord (OBRIGATÓRIA)
WEBHOOK_URL="https://discord.com/api/webhooks/xxxxx/xxxxx"

# Fonte de dados: rss | json | forum
# - rss: usa RSS_URL para ler itens RSS
# - json: usa COUPONS_JSON_URL (array de objetos com id/title/link)
# - forum: usa FORUM_URL_UPDATES para fazer scraping da página HTML
SOURCE_PROVIDER="rss"

# RSS
RSS_URL="https://hnrss.org/frontpage" # apenas exemplo para validar

# JSON (apenas se SOURCE_PROVIDER=json)
COUPONS_JSON_URL="" # ex.: https://raw.githubusercontent.com/user/repo/main/coupons.json

# Fórum (apenas se SOURCE_PROVIDER=forum)
FORUM_URL_UPDATES="https://forum.netmarble.com/sk_rebirth_gl/list/11/1" # página de Updates
# Palavras-chave usadas para filtrar títulos de posts relevantes
# Separe por vírgula. Se vazio, não filtra.
FORUM_INCLUDE_KEYWORDS="coupon,cupom,redeem,code,codigo"
# Número de páginas da lista para verificar (paginação)
# Ex.: 2 para cobrir as duas primeiras páginas de Updates
FORUM_MAX_PAGES="2"

# Intervalo de verificação (minutos)
CHECK_INTERVAL="5"

# Formato da mensagem enviada ao Discord
MESSAGE_FORMAT="Novo cupom: {{title}} - {{link}}"

# Arquivo para deduplicação (persistência simples)
STORAGE_FILE="data.json"