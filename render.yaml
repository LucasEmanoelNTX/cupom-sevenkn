services:
  - type: web
    name: cupom-sevenkn
    env: node
    plan: free
    buildCommand: pnpm install --frozen-lockfile
    startCommand: pnpm start
    autoDeploy: true
    healthCheckPath: /health
    envVars:
      - key: SOURCE_PROVIDER
        value: rss
      - key: RSS_URL
        value: https://hnrss.org/frontpage
      - key: COUPONS_JSON_URL
        value: 
      - key: WEBHOOK_URL
        sync: false
      - key: CHECK_INTERVAL
        value: "5"
      - key: MESSAGE_FORMAT
        value: "Novo cupom: {{title}} - {{link}}"
      - key: STORAGE_FILE
        value: data.json
      - key: FORUM_URL_UPDATES
        value: 
      - key: FORUM_INCLUDE_KEYWORDS
        value: "coupon,cupom,redeem,code,codigo"